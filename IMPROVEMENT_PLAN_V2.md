# 改进方案：律师办案体验增强

基于个人律师实际办案场景，按**使用频率×价值**排序，分3个阶段推进。

---

## 阶段A：高频刚需（1-2天）

### A1. 诉讼时效提醒
**痛点**：律师容易错过起诉期限、仲裁时效等关键日期，后果严重。

**方案**：
- 案件详情页新增"期限管理"卡片
- 创建案件时根据案由自动推荐关键期限（如：劳动仲裁1年、普通诉讼3年）
- 支持手动添加自定义期限
- 到期前N天（可配置）在首页置顶提醒
- 已过期期限标红警示

**改动范围**：
- `backend/routers/cases.py` — 新增期限 CRUD 接口
- `backend/models/deadline.py` — 已有模型，需扩展字段（reminder_days、is_completed）
- `frontend/src/pages/CaseDetail.tsx` — 期限管理卡片 UI
- `frontend/src/pages/CaseList.tsx` — 首页过期提醒横幅

### A2. 文书模板库
**痛点**：每次写文书都要从零开始，缺乏标准格式参考。

**方案**：
- 新增"文书模板"页面，预置常见法律文书模板
- 模板分类：起诉状、答辩状、代理词、律师函、法律意见书等
- 每个模板包含：标准格式、必备条款、常见注意事项
- 律师可基于模板快速创建文书，系统自动填充案件信息
- 支持律师自定义模板并保存

**改动范围**：
- `backend/routers/templates.py` — 扩展模板 CRUD
- `backend/models/case_template.py` — 已有模型
- `frontend/src/pages/TemplateSelector.tsx` — 增强模板选择器
- `frontend/src/pages/ModelConfig.tsx` — 新增"文书模板"标签页

---

## 阶段B：效率提升（2-3天）

### B1. 案件搜索增强
**痛点**：案件多了以后难以快速定位。

**方案**：
- 搜索支持关键词高亮显示
- 搜索结果按相关性排序（案件名称 > 描述 > 案号）
- 最近搜索历史（localStorage 存储，最多10条）
- 快捷键 `/` 聚焦搜索框
- 搜索结果数量统计

**改动范围**：
- `frontend/src/pages/CaseList.tsx` — 搜索增强 UI

### B2. 导出前预览
**痛点**：导出 Word 后才发现格式问题，需要重新修改再导出。

**方案**：
- 导出前弹出预览弹窗
- 显示文书排版效果（标题、正文、证据目录的布局）
- 可选择导出格式：标准排版 / 法院严格格式
- 预览中可快速跳转到对应阶段修改

**改动范围**：
- `frontend/src/components/ExportOptionsModal.tsx` — 已有组件，增强预览
- `frontend/src/pages/WorkflowPage.tsx` — 导出按钮交互优化

### B3. 新手引导
**痛点**：首次使用时不知道从哪里开始。

**方案**：
- 首次登录弹出3步引导：
  1. 创建案件（填写案由、委托目标）
  2. 上传材料（合同、流水、聊天记录等）
  3. 进入工作流（按阶段生成文书）
- 引导完成后不再显示（localStorage 标记）
- 空案件列表时显示引导卡片

**改动范围**：
- `frontend/src/pages/CaseList.tsx` — 引导流程 UI

---

## 阶段C：专业增强（3-5天）

### C1. 文件在线预览
**痛点**：上传材料后无法在线查看，需要下载到本地。

**方案**：
- 上传材料列表中添加"预览"按钮
- PDF 使用浏览器内置预览
- 图片直接展示
- Word 文档显示解析后的纯文本
- 预览模态框支持翻页（多页PDF）

**改动范围**：
- `backend/routers/materials.py` — 新增文件预览接口（返回内容/图片流）
- `frontend/src/pages/CaseDetail.tsx` — 预览模态框

### C2. 批量操作增强
**痛点**：只能批量删除，无法批量归档或切换状态。

**方案**：
- 案件列表支持复选框多选
- 批量操作栏：删除 / 归档 / 标记完成
- 操作前二次确认
- 操作后刷新列表

**改动范围**：
- `frontend/src/pages/CaseList.tsx` — 批量操作 UI
- `backend/routers/cases.py` — 新增批量归档接口

### C3. 审计日志导出
**痛点**：审计日志只能在线查看，无法存档。

**方案**：
- 审计日志页面添加"导出Excel"按钮
- 导出字段：时间、操作、资源类型、资源ID、摘要
- 按时间范围筛选后导出

**改动范围**：
- `backend/routers/audit.py` — 新增导出接口（生成CSV）
- `frontend/src/pages/AuditLogPage.tsx` — 导出按钮

---

## 执行优先级

| 优先级 | 改进项 | 预计工时 | 价值 |
|--------|--------|----------|------|
| P0 | A1 诉讼时效提醒 | 1天 | 极高（避免过期风险） |
| P0 | A2 文书模板库 | 1天 | 极高（提升效率） |
| P1 | B1 搜索增强 | 0.5天 | 高（案件多时必需） |
| P1 | B3 新手引导 | 0.5天 | 高（降低上手门槛） |
| P1 | B2 导出预览 | 1天 | 中高（减少返工） |
| P2 | C1 文件预览 | 1天 | 中（提升体验） |
| P2 | C2 批量操作 | 0.5天 | 中（效率提升） |
| P2 | C3 日志导出 | 0.5天 | 低（审计需求） |

**建议**：先做 P0 的 A1 + A2，预计 2 天完成，对律师日常办案帮助最大。
